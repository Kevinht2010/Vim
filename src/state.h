#ifndef STATE_H
#define STATE_H

enum State {
  NORMAL_MODE = 0,
  INSERT_MODE,
  DEL_MODE,
  CHANGE_MODE,
  COPY_MODE,
  FIND_MODE,
  RFIND_MODE,
  MULT_MODE,
  FIND_CHARACTER_MODE,
  RFIND_CHARACTER_MODE,
  REPLACE_MODE,
  REPLACE_CHARACTER_MODE,
  COMMAND_MODE
};

#endif
